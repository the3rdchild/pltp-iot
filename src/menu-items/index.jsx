// project import
import dashboard from './dashboard';
import analytics from './analytics';
import settings from './settings';
import logout from './logout';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, analytics, settings, logout]
};

export default menuItems;
